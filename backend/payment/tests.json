{
  "tests": [
    {
      "name": "OPTIONS CORS",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200
    },
    {
      "name": "GET status — статус платёжек",
      "method": "GET",
      "path": "/?action=status",
      "expectedStatus": 200,
      "expectedBody": {"robokassa": {}, "yookassa": {}, "plans": {}},
      "bodyMatcher": "partial"
    },
    {
      "name": "POST create_payment — нет сессии",
      "method": "POST",
      "path": "/",
      "body": {"action": "create_payment", "plan": "basic", "provider": "robokassa"},
      "expectedStatus": 401,
      "expectedBody": {"error": "Не авторизован"},
      "bodyMatcher": "partial"
    }
  ]
}
